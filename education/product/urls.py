from django.urls import path


app_name = 'product'
urlpatterns = [
    # path('', MainPageView.as_view(), name="index"),
    # path('api/lesson/', LessonActiveAPIView.as_view()),
    # path('api/product/', ProductReviewView.as_view()),
]